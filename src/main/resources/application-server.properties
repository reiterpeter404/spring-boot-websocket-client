spring.application.name=websocket-client
server.port=${random.int(8889,9090)}

websocket.authentication.url=${websocketConnectionUrl}
websocket.authentication.username=${username}
websocket.authentication.password=${password}
websocket.authentication.jwt_secret=${jwtToken}
